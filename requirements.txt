fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic-settings>=2.10.1,<3
supabase==2.30.1
websockets>=13,<16
redis==5.0.4
google-generativeai==0.7.0
google-cloud-aiplatform==1.60.0
openai>=2.45.0,<3
python-dotenv==1.0.1
pytest==8.2.0
pytest-asyncio==0.23.7
fakeredis==2.23.0
pytest-mock==3.14.0
pytest-timeout==2.3.1
httpx>=0.27.1,<0.29
composio-core>=0.7.0
networkx>=3.3
scipy>=1.13.0  # networkx.pagerank's default (fast, sparse-matrix) codepath requires it
yt-dlp>=2024.1.0
youtube-transcript-api>=0.6.2
psutil>=6.0.0
ddgs>=7.0.0
langchain==1.3.14
langchain-core==1.5.0
langgraph==1.2.9
langchain-community==0.4.2
langchain-openai==1.4.0
langchain-deepseek==1.1.0
langchain-mcp-adapters==0.3.0
langsmith==0.10.9
mcp==1.28.1
tavily-python==0.7.26
aiohttp>=3.10.0
requests>=2.31.0
beautifulsoup4>=4.12.0
trafilatura>=1.12.0
playwright>=1.45.0
pypdf==4.3.1

fpdf2>=2.7.0
python-pptx>=0.6.21
pywebpush>=1.14,<2

# Wave 1 control plane (PLAN.md): durable workflow engine for the future
# Temporal execution shell. Not wired into any running code path yet --
# scaffolding only (backend/control_plane/temporal/). Loose pin: not
# confident of the exact latest patch version from this sandbox, tightened
# once someone actually deploys against it.
temporalio>=1.7,<2

# Wave 5.2 control plane (PLAN.md): mandatory OTel tracing (backend/observability/
# tracing.py). Loose major-version pin, matching the temporalio convention above --
# chromadb already pulls opentelemetry-api/sdk/otlp-proto-grpc transitively on 1.x,
# so this stays inside that same major line rather than pinning an exact patch.
opentelemetry-api>=1.27,<2
opentelemetry-sdk>=1.27,<2
opentelemetry-exporter-otlp-proto-http>=1.27,<2
graphiti-core[falkordblite]>=0.17.0
sentence-transformers>=3.0.0
